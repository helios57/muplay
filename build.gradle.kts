// Intentionally empty. Every module applies its plugins through a build-logic convention plugin
// (`muplay.*`, defined in build-logic/convention), which applies the underlying AGP/Kotlin/
// KSP/Hilt plugins itself with an explicit, catalogue-pinned version. Nothing needs declaring
// here.

package app.muplay.designsystem.theme

import androidx.compose.ui.graphics.Color

// A small, deliberately plain Material 3 palette — this task only needs a theme that exists and
// is genuinely applied, not a bespoke brand identity. Widening this is a design pass for later.

val MuPlayPrimaryLight = Color(0xFF6750A4)
val MuPlayOnPrimaryLight = Color(0xFFFFFFFF)
val MuPlaySecondaryLight = Color(0xFF625B71)
val MuPlayOnSecondaryLight = Color(0xFFFFFFFF)
val MuPlayTertiaryLight = Color(0xFF7D5260)
val MuPlayErrorLight = Color(0xFFBA1A1A)

val MuPlayPrimaryDark = Color(0xFFD0BCFF)
val MuPlayOnPrimaryDark = Color(0xFF381E72)
val MuPlaySecondaryDark = Color(0xFFCCC2DC)
val MuPlayOnSecondaryDark = Color(0xFF332D41)
val MuPlayTertiaryDark = Color(0xFFEFB8C8)
val MuPlayErrorDark = Color(0xFFFFB4AB)
